# Docker embedded DNS for service discovery from within the nginx container.
resolver 127.0.0.11 valid=10s ipv6=off;
resolver_timeout 2s;

upstream app_backend {
    zone app_backend 64k;
    least_conn;
    server web:8000 resolve;
    keepalive 64;
}
